version: "2"
sql:
  - engine: "sqlite"
    queries: "./database/query"
    schema: "./database/migrate"
    gen:
      go:
        package: "repo"
        out: "./repo"
        emit_pointers_for_null_types: true
        emit_interface: true
        emit_json_tags: true
